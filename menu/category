{.section items}
  <nav>
    <ul>
    {.repeated section @}
      <li class="{.section active}active{.end}
                 {.section disabled}disabled{.end}">
        {.section disabled}
          <span class="title">{text|html}</span>
        {.or}
          <a class="title" href="{url|htmltag}">{text|html}</a>
        {.end}
        {.section items}
          <ul>
            {.section description}
              <legend>{description|html}</legend>
            {.end}
            {.repeated section items}
              <li class="sub {.section active}active{.end}
                         {.section disabled}disabled{.end}">
              {.section disabled}
                <span class="title">{text|html}</span>
              {.or}
                <a class="title" href="{url|htmltag}">{text|html}</a>
              {.end}
              </li>
            {.end}
          </ul>
        {.end}
      </li>
    {.end}
    </ul>
  </nav>
{.end}
